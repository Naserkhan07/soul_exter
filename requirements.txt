# Core (always required)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1

# Optional — enable as needed:
# datasets>=2.19            # Hugging Face company dataset streaming
# praw>=7.7                 # Reddit API (official, read-only research)
# playwright>=1.44          # JS-heavy site rendering (then: playwright install chromium)
# pandas>=2.0               # bulk data wrangling / notebook analysis

# Kaggle GPU brain (install on Kaggle, not needed locally):
# torch
# transformers>=4.41
# accelerate
# bitsandbytes              # 4-bit quantization for Qwen 3B
